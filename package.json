{
  "name": "outrun",
  "private": true,
  "version": "0.1.0",
  "description": "Outrun: an interactive laboratory for Conway's Angel Problem and other infinite-grid systems",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/selftest.mjs",
    "check": "npm test && npm run build",
    "deploy": "bash scripts/deploy.sh",
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
